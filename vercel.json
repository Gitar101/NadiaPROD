{
  "devCommand": "uvicorn main:app --host 0.0.0.0 --port 5000",
  "builds": [
    {
      "src": "batch.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "batch.py"
    }
  ]
}
